#include <stdio.h>
#include <stdlib.h>
struct alumnos {
char nombre[40];
int edad;
float promedio;


} alumno1,alumno2;
int main()
{
    printf("Ingresa el nombre del alumno #1: ")
    gets(alumno1.nombre);
    fflush(stdin);
    printf("Ingresa la edad del alumno #1 : ")
    scanf()
    system("pause");
    return 0;
}
